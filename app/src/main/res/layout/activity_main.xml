<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/ic_main_background"
    tools:context="com.example.admin.testvk.MainActivity">
        <Button
            android:id="@+id/authButton"
            android:layout_width="250dp"
            android:layout_height="48dp"
            android:layout_gravity="center"
            android:text="@string/authorize_title"
            android:textColor="@android:color/white"
            android:paddingLeft="16dp"
            android:drawableLeft="@drawable/ic_vk_social_network_logo"
            android:background="#5a80aa"
            android:drawableStart="@drawable/ic_vk_social_network_logo"
            android:paddingStart="16dp" />
</FrameLayout>
